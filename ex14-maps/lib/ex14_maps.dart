/// Lesson 14 — Maps.
///
/// A `Map<K, V>` is a key → value table. Access with `m[key]`, add
/// with `m[key] = value`, iterate with `.forEach` or `.entries`.

/// Return a map with keys `name`, `profession`, `country`, `city`
/// pre-filled with any values you like.
Map<String, String> personInfo() {
  throw UnimplementedError('TODO');
}

/// Return a **new** map based on [m] with its `city` entry set to
/// [city]. Must not mutate the argument.
Map<String, String> updateCity(Map<String, String> m, String city) {
  throw UnimplementedError('TODO');
}

/// Return the number of keys in [m].
int countKeys(Map<String, Object?> m) {
  throw UnimplementedError('TODO');
}

/// Return the values of [m] as a list, in the map's iteration order.
List<String> valuesOf(Map<String, String> m) {
  throw UnimplementedError('TODO');
}
