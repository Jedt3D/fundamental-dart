/// Lesson 05 — Control flow.
///
/// Dart has the usual toolkit: `if`/`else`, `switch`, and the ternary
/// `cond ? a : b`. There's also `??` ("if null, use that"), which we'll
/// meet properly in lesson 11.

/// Return the classic FizzBuzz string for [n]:
///   multiples of 3   -> 'Fizz'
///   multiples of 5   -> 'Buzz'
///   multiples of 15  -> 'FizzBuzz'
///   otherwise        -> n as a string, e.g. '7'.
String fizzbuzz(int n) {
  throw UnimplementedError('TODO');
}

/// Return `true` when [age] is between 13 and 19 (inclusive).
bool isTeenager(int age) {
  throw UnimplementedError('TODO');
}

/// Use a `switch` statement to classify:
///   'rock'     -> 'heavy'
///   'paper'    -> 'flimsy'
///   'scissors' -> 'sharp'
///   anything else -> 'unknown'.
String classify(String item) {
  throw UnimplementedError('TODO');
}

/// Use the ternary operator (`? :`) to return `|a - b|`.
int absDiff(int a, int b) {
  throw UnimplementedError('TODO');
}
