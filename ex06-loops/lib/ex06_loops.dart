/// Lesson 06 — Loops.
///
/// Dart has three loop forms: `for`, `for-in`, and `while`.
/// Plus the usual `break` and `continue` to cut a loop short.

/// Return `n!` (n factorial). By convention `factorial(0) == 1`.
/// Use a `for` loop.
int factorial(int n) {
  throw UnimplementedError('TODO');
}

/// Return the sum of the decimal digits of [n], using a `while` loop.
/// Example: `sumDigits(1729) == 1 + 7 + 2 + 9 == 19`.
int sumDigits(int n) {
  throw UnimplementedError('TODO');
}

/// Return the list `[1, 4, 9, ..., n*n]`. Empty when `n <= 0`.
List<int> firstNSquares(int n) {
  throw UnimplementedError('TODO');
}

/// Return the smallest power of two that is >= [n].
/// `nextPowerOfTwo(1) == 1`, `nextPowerOfTwo(5) == 8`,
/// `nextPowerOfTwo(16) == 16`.
int nextPowerOfTwo(int n) {
  throw UnimplementedError('TODO');
}
