/// Lesson 03 — Types and operators.
///
/// Dart has `int`, `double`, `num` (the parent of both), `bool`, and
/// `String`. Arithmetic follows the usual precedence rules. Dividing
/// two `int`s with `/` always yields a `double`; use `~/` for integer
/// division.

/// Return the mean of [a] and [b] as a `double`.
double average(int a, int b) {
  throw UnimplementedError('TODO: (a + b) / 2');
}

/// Return a letter grade for a 0..100 score:
///   90+ => 'A'   80+ => 'B'   70+ => 'C'
///   60+ => 'D'   else => 'F'.
String letterGrade(int score) {
  throw UnimplementedError('TODO: use if/else');
}

/// Return the sum of the integers 1..n inclusive.
/// (Use the closed form or a loop — either is fine.)
int sumUpTo(int n) {
  throw UnimplementedError('TODO');
}

/// Return `true` when [n] is even. Use the `%` operator.
bool isEven(int n) {
  throw UnimplementedError('TODO');
}
