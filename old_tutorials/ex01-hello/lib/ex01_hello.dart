/// Lesson 01 — Hello, Dart.
///
/// Implement each of the functions below so that `dart test`
/// turns green. Delete the `throw UnimplementedError(...)`
/// lines as you go.

/// Return the greeting `"Hello, <name>!"`.
///
/// Example: `greet('Ada') == 'Hello, Ada!'`.
String greet(String name) {
  throw UnimplementedError('TODO: return "Hello, <name>!"');
}

/// Return the greeting from [greet] but entirely in UPPER CASE.
///
/// Example: `greetLoud('Ada') == 'HELLO, ADA!'`.
String greetLoud(String name) {
  throw UnimplementedError('TODO: reuse greet() then uppercase it');
}

/// Return the Hitchhiker's Guide answer to life, the universe and
/// everything. It happens to be `6 * 7`.
int answerToLife() {
  throw UnimplementedError('TODO: compute 6 * 7');
}
